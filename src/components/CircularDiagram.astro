---
import CircularDiagramPC from './CircularDiagramPC.astro';
import CircularDiagramMobile from './CircularDiagramMobile.astro';

interface Props {
	centerTitle: string;
	centerText: string;
	leftTitle: string;
	leftText: string;
	rightTitle: string;
	rightText: string;
}

const props = Astro.props;
---

<!-- Combined Circular Diagram Component -->
<CircularDiagramPC {...props} />
<CircularDiagramMobile {...props} />